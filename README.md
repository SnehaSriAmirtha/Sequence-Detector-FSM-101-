# Sequence-Detector-FSM-101-
: Design a finite state machine that detects the sequence '101' in a serial input stream. Implement both overlapping and non-overlapping versions. Create Moore and Mealy machine implementations. 
